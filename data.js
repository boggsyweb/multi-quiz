export const multiQuiz = [
    
    {
   question: "Which of the following is NOT a primitive data type in JavaScript?",
    choices: ["boolean", "string", "array"],
    answer:"array"
    },
    {
    question: "Which of the following ways to declare a variable in JavaScript has been deprecated?",
    choices: ["var", "let", "const"],
    answer:"var"
    },
{
    question: "How do you write an 'if statement' in JavaScript?",
    choices: ["if (hour < 12) { greeting = 'good morning!'}", "if [hour < 12] {greeting = 'good morning'}", "if (hour < 12; greeting = 'good morning!')"],
    answer: "if (hour < 12) { greeting = 'good morning!'}"
},
{
    question: "What is the correct way to write a 'for loop' in JavaScript?",
    choices: ["for (let i = 0; i < array.length; i+){code to be executed}", "for (let i = 0; i < array.length; i++){code to be executed}", "(let i = 0; i < array.length; i++){code to be executed}"],
    answer: "for (let i = 0; i < array.length; i++){code to be executed}"
},
{
    question: "Which of the following is the correct way of defining a function in JavaScript?",
    choices: ["myFunction{code to be executed}", "myFunction() {code to be executed} function", "function myFunction() {code to be executed}"],
    answer: "function myFunction() {code to be executed}"
},
{
    question: "For which of the following would you expect to see 'false' logged out?",
    choices: ["console.log(0 == false)", "console.log(1 === '1')", "console.log(1 == '1')"],
    answer: "console.log(1 === '1')"
}

]